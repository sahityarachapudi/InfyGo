spring:
  mvc:
    view:
      prefix: /WEB-INF/pages/
      suffix: .jsp
  datasource:
    username: root
    password: infy@123
    url: jdbc:mysql://localhost:3306/passenger
  profiles:
    active:
    - dev
server:
  servlet.context-path: /InfyGoBoot
  port: 4343
  
logging:
  file: Errorlog.log
  level:
    root: info
    com.infosys.irs: info
  pattern:
    file: '%d{yyyy-MM-dd HH:mm:ss,SSS} %5p [%t] %c [%M] - %m%n'
    
   
